export interface MailResponse {
    status_code: number;
    status_msg: string;
}
export interface AvaterResponse {
    status_code: number;
    status_msg: string;
}

//发送邮件去修改密码
export interface ResetResponse {
    status_code: number;
    status_msg: string;
}